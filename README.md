# Auth System

A simple user authentication system built with Node.js, Express, JWT, and bcrypt. This project allows users to register, login, and access protected routes using JWT tokens.

## Features

- User registration
- User login
- Protected route access with JWT

## Installation

Clone the repository:
```bash
git clone https://github.com/SmitaAmrutkar/auth-system.git
